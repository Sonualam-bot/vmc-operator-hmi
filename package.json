{
  "name": "vmc-operator-hmi",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "VMC Operator HMI - Startup Guidance (Primeform Labs technical assignment)",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "build": "npm run build --workspace=@vmc-hmi/shared && npm run build --workspace=@vmc-hmi/server && npm run build --workspace=@vmc-hmi/client",
    "test": "vitest run",
    "dev:server": "npm run dev --workspace=@vmc-hmi/server",
    "start": "npm run start --workspace=@vmc-hmi/server"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
